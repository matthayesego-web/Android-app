name: TornFCA v0.10.18 Signed Beta

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  signing-paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Signing is intentionally paused while beta bug fixing continues. Restore the verified signing workflow only after explicit approval.'
