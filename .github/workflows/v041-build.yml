name: Build v0.4.3 Image Fix

on:
  push:
    branches:
      - agent/v043-image-fix
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '8.13'
      - name: Build debug and unsigned release APKs
        run: gradle --no-daemon :app:assembleDebug :app:assembleRelease
      - name: Name APKs
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk DuckForce-Companion-v0.4.3-debug.apk
          cp app/build/outputs/apk/release/app-release-unsigned.apk DuckForce-Companion-v0.4.3-unsigned-release.apk
      - uses: actions/upload-artifact@v7
        with:
          name: DuckForce-Companion-v0.4.3
          path: |
            DuckForce-Companion-v0.4.3-debug.apk
            DuckForce-Companion-v0.4.3-unsigned-release.apk
          if-no-files-found: error
          retention-days: 30
